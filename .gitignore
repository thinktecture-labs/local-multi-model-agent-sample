# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Model directories — all GGUFs and weights are downloaded/trained locally
models/

# Evaluation results (generated by eval scripts)
results/

# llama.cpp submodule build artifacts (binary is built locally)
vendor/llama.cpp/build/

# whisper.cpp (built by scripts/setup_voice.sh)
vendor/whisper.cpp/

# Generated databases (created at runtime by data/loader.py)
data/business.db
data/chroma/
data/chromadb/
chroma_db/

# Training checkpoints
checkpoints/

# Pytest cache
.pytest_cache/

# Claude Code
.claude/

# Environment config (copy .env.example to .env, override with .env.local)
.env
.env.local

# Runtime demo interaction log (grows during demo usage)
data/interactions.json

# llama-server background PIDs
.server-pids
.server-pids-ft

# Node (Observatory UI)
node_modules/

# Wake word ONNX models (copied from node_modules by setup.sh)
src/clients/observatory-react/public/openwakeword/

# OS
.DS_Store
Thumbs.db

# show_stack.sh GGUF metadata cache (mtime-keyed)
scripts/.cache/
