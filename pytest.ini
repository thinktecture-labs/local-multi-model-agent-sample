[pytest]
asyncio_mode = auto
testpaths = tests

markers =
    unit:        fast isolated tests — no external services required
    integration: tests that require local SQLite but no model servers
    e2e:         full-pipeline tests that require llama-server on ports 9090/9091/9092

# Show slowest tests
addopts = -v --tb=short
